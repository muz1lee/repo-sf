# 复制为 `.env`，或在 shell 中 export 对应变量。
# 不要提交真实 API key、service token 或私有 endpoint。

# 本项目没有确认的 SAM3D 默认 endpoint。
# 只有在当前服务注册表确认后，才设置这个变量。
SAM3D_PROCESS_URL=

# 可选 VLM / proposal 服务配置。
QWEN_BASE_URL=
QWEN_API_KEY=
QWEN_MODEL=

# 可选 inpainting 服务。CLI 也支持显式传入 --inpaint-url。
RSF_INPAINT_URL=
